name: Build Godot Engine for Android

on:
  push:
    branches:
      - main  # Trigger the workflow on pushes to the main branch
  pull_request:
    branches:
      - main  # Trigger the workflow for pull requests targeting main

jobs:
  build:
    runs-on: ubuntu-latest  # Use an Ubuntu environment for the build process

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: 11  # Godot requires Java to build for Android

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 python3-pip clang build-essential scons git wget

      - name: Install Android SDK and NDK
        run: |
          sudo apt-get install -y android-sdk android-ndk

      - name: Download Godot Export Templates
        run: |
          mkdir -p godot_export
          cd godot_export
          wget https://downloads.tuxfamily.org/godotengine/3.6/export_templates/godot-3.6-stable-export-templates.tpz
          tar -xvzf godot-3.6-stable-export-templates.tpz
          cd ..

      - name: Build Godot Engine for Android
        run: |
          cd android/godot_source_code  # Navigate to where your Godot source code is located
          scons platform=android target=release tools=yes  # Build the engine for Android

      - name: Upload Build Artifacts (APK)
        uses: actions/upload-artifact@v2
        with:
          name: godot-android-apk
          path: android/godot_source_code/bin/godot.apk
